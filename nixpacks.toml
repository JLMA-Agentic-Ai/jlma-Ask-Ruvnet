[phases.setup]
nixPkgs = ["nodejs-18_x", "python3", "ffmpeg", "gnumake", "gcc"]

[phases.install]
cmds = ["npm ci", "cd src/ui && npm ci"]

[phases.build]
cmds = ["cd src/ui && npm run build"]

[start]
cmd = "bash scripts/deployment/start-railway.sh"
